tasks:
        - exec:
                client.salt_master:
                        - 'ceph -s'
        - deepsea.validation:
        - salt.command:
                command: 'state.apply ceph.updates.salt'
        - deepsea.orch:
                state_orch: ceph.maintenance.upgrade
